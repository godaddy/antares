---
'@godaddy/antares': minor
---

feat(antares): add DateField and DateRangeField components

Calendar-popover-first date inputs. By default they show a localized value (e.g. `12 Aug 2022`)
and open a Calendar / RangeCalendar on click; set `allowsKeyboardInput` for editable segments.
Also exports `I18nProvider` and includes FieldFrame focus/layout fixes.
