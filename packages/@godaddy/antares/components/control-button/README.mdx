---
title: ControlButton
description: Pressable control for use inside a Group. Field paints it via ControlButtonContext so page Buttons stay unchanged.
---

import { ArgTypes, Meta } from '@storybook/addon-docs/blocks';
import { Examples } from '@bento/storybook-addon-helpers/runtime';
import * as Stories from './control-button.stories.tsx';

<Meta of={Stories} name="Overview" />

## Features

- Use for NumberField steppers, Select/DatePicker triggers, and in-group actions.
- Use `ControlButton` for those slots instead of `Button`, so page buttons keep their own styles.

## Installation

```bash
npm install --save @godaddy/antares
```

## Examples

<Examples of={Stories.Examples} />

## Props

<ArgTypes of={Stories.Props} />
