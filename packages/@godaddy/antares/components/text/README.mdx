---
title: Text
description: Text component for displaying and formatting text content with alignment and truncation options
---
import { ArgTypes, Meta } from '@storybook/addon-docs/blocks';
import * as Stories from './text.stories.tsx';

<Meta of={Stories} name="Overview" />

The `Text` component prepares content with a few properties.

## Installation

```bash
npm install --save @godaddy/antares
```

## Examples

<Examples of={Stories.Examples} />

## Props

The `Text` component accepts the following props:

<ArgTypes of={Stories.Props} />
