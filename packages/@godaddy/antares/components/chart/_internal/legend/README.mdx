---
title: Legend
description: The Legend component is an internal component used for displaying series information in chart components.
---
import { Meta, ArgTypes, Source, Story } from '@storybook/addon-docs/blocks';
import * as Stories from './legend.stories.tsx';

import SourceSingleSeries from './examples/single-series.tsx?raw';
import SourceMultiSeries from './examples/multi-series.tsx?raw';
import SourceWithLabel from './examples/with-label.tsx?raw';
import SourceVertical from './examples/vertical.tsx?raw';
import SourceLegendSizeChart from './examples/legend-size-chart-example.tsx?raw';

<Meta of={Stories} name="Overview" />

## Internal Component

This component is **internal-only** and not exported from the package. It is designed for use within chart components.

## Props

The `Legend` component accepts the following props:

<ArgTypes of={Stories.Props} />

## Examples

### Single Series

Basic legend with a single series showing the default styling and layout.

<Source language='tsx' code={SourceSingleSeries} />
<Story of={Stories.SingleSeries} inline />

### Multiple Series

Legend with multiple series showing theme colors and automatic spacing. Each series has its own colored indicator and name.

<Source language='tsx' code={SourceMultiSeries} />
<Story of={Stories.MultipleSeries} inline />

### With label

Legend with a visible label that is also used as the accessible label.

<Source language='tsx' code={SourceWithLabel} />
<Story of={Stories.WithLabel} inline />

### Vertical orientation

Legend items stacked vertically while keeping swatch and label side-by-side, useful when horizontal space is constrained.

<Source language='tsx' code={SourceVertical} />
<Story of={Stories.Vertical} inline />

### Legend size chart example

Legend with all supported sizes (`sm`, `md`, `lg`, `xl`).

<Source language='tsx' code={SourceLegendSizeChart} />
<Story of={Stories.LegendSizeChart} inline />
