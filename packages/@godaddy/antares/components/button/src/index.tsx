/**
 * The Button family. Each component lives in its own file:
 *
 * - `Button` / `LinkButton` -> `./button.tsx`
 * - `CloseButton` (a `Button` preset) -> `./close-button.tsx`
 */

export { Button, type ButtonProps, LinkButton, type LinkButtonProps } from './button.tsx';
export { CloseButton, type CloseButtonProps } from './close-button.tsx';
