import { Meta, Story, ArgTypes, Controls, Source } from '@storybook/addon-docs/blocks';
import ButtonExample from './examples/button.tsx?raw';
import * as Stories from './button.stories.tsx';

<Meta of={Stories} name="Overview" />

***REMOVED***Button

The `@bento/button` package exports the **Button** component, which is a complete button component built on top of the [**Pressable**](/docs/components-pressable--overview) primitive.

#***REMOVED***Installation

```bash
npm install --save @bento/button
```

#***REMOVED***Props

The following properties are available to be used on the `Button` component:

<ArgTypes of={Stories.Props} />

#***REMOVED***Examples

<Source language="tsx" code={ButtonExample} />
