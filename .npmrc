# Default registry for all packages
registry=https://registry.npmjs.org/

# Allow saving dependencies with semver ranges (^, ~, etc.)
save-exact=false

# Use npm workspaces
workspaces-update=true

# Audit settings
audit-level=moderate

# CI authentication - uses NPM_TOKEN environment variable
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
